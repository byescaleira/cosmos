# Tracking

Opt-in, passive analytics via ``CosmosTrackingConfiguration``. `track(_:)` uses
`componentId = trackingId ?? accessibilityId`. No network, no PII.

## Topics

- ``CosmosTrackingConfiguration``
- ``CosmosTrackingEvent``